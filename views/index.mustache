<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Text Saver</title>
  <link rel="stylesheet" href="public/style.css">
  <link href="https://fonts.googleapis.com/css?family=Roboto+Slab" rel="stylesheet">
</head>

<body>
  <div class="container">

    <header>
      <h1>Give me some Text!</h1>
    </header>
    <p class="errorMsg">{{error}}</p>
    <form class="textForm" action="/" method="post" >
        <input type="text" name="title" value="" placeholder="Title" aria-describedby="title-tip" label = "Title"/>
        <div role="tooltip" id="title-tip">
          Input the title of your entry
        </div>
      <textarea name="textBody" rows="8" cols="80" placeholder="Write here!" aria-describedby="textBody-tip" label = "TextBody"></textarea>
      <div role="tooltip" id="textBody-tip">
        Input your your entry minimum 5 characters maximum 500 characters.
      </div>
      <button type="submit" name="button">Save Text</button>
    </form>

    <div class="blockSpace" >

      {{#Blocks}}
        <div class="textBlock" role = "tabpanel">

          <h1>{{title}}</h1>
          <p>{{textBody}}</p>

        </div>
      {{/Blocks}}

    </div>
  </div>

</body>

</html>
